int func (int x) {
	if (x > 1)
		return x + func(x - 1);
	else
		return 1;
}

void main (void) {
	int req;
	int sol;
	int i;

	while ( i < 3) {
		req = input();
		sol = func(req);
		output(sol);
		i = i + 1;
	}
}
